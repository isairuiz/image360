<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Textbox" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/redondeado</item>
        <item name="android:textColorHint">#635555</item>
        <item name="android:textColor">@color/colorBoxtext</item>
        <item name="android:singleLine">true</item>
        <item name="android:layout_marginTop">@dimen/box_top_margin</item>
        <item name="android:paddingLeft">@dimen/box_left_padding</item>
    </style>

</resources>
